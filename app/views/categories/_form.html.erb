<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :material %>
    <%= form.text_field :material, id: :category_material %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color, id: :category_color %>
  </div>

  <%= form.fields_for :variation do |f| %>
    
  <div class="field">
   V color: <%= f.label :color %>
    <%= f.text_field :color, id: :variation_color %>
  </div>

  <div class="field">
   V material: <%= f.label :material %>
    <%= f.text_field :material, id: :variation_material %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  <% end %>

<% end %>
